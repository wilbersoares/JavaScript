var formulario = document.querySelectorAll("#form-calc-dados");

var nome = formulario.nome.value;
var data = formulario.data.value;

nome.textContent = nome;
data.textContent = data;

console.log(nome);
console.log(data);